program main(input,output);
 var a,b,c: integer;
 var x,y,z: real;
 var p: array[3 .. 11] of integer;
 var q: array[1 .. 10] of real;
 function foo(x: integer; b:real):integer;
 begin
 end;
 function moo(y:integer):real;
 begin
 end;
begin
 a := foo(p[b],moo(b));
 if ( not (a < b) ) then
     c := 13
end.
